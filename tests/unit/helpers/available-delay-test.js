import {
  availableDelay
} from '../../../helpers/available-delay';
import { module, test } from 'qunit';

module('AvailableDelayHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = availableDelay(42);
  assert.ok(result);
});
